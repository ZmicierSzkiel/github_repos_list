part of app_theme;

class AppColors {
  const AppColors();

  static const Color transparent = Colors.transparent;

  // Accent
  static const Color celestialAlien = Color.fromRGBO(12, 197, 9, 1);

  // Background
  static const Color washMe = Color.fromRGBO(250, 250, 252, 1);

  // Layers
  static const Color white26 = Color.fromRGBO(255, 255, 255, 0.26);
  static const Color alpineGoat = Color.fromRGBO(241, 242, 246, 1);
  static const Color celestialAlien05 = Color.fromRGBO(12, 197, 9, 0.05);
  static const Color bleachedSilk = Color.fromRGBO(242, 242, 242, 1);

  // Text
  static const Color nobleBlack = Color.fromRGBO(28, 32, 39, 1);
  static const Color silver = Color.fromRGBO(191, 191, 191, 1);

  // System
  static const Color bloodDonor = Color.fromRGBO(234, 26, 26, 1);

  // Icon
  static const Color nobleBlackIcon = Color.fromRGBO(28, 32, 39, 1);
}
