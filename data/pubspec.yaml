name: data

publish_to: none

environment:
  sdk: ">=3.4.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
    
  core:
    path: ../core
  domain:
    path: ../domain

dev_dependencies:
  flutter_lints: ^3.0.0
