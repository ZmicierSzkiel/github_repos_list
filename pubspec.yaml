name: github_repos_list
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.1 <4.0.0"
dependencies:
  flutter:
    sdk: flutter

  app_theme:
    path: app_theme
  core:
    path: core
  data:
    path: data
  navigation:
    path: navigation

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0